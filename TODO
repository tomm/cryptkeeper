idle auto-unmount leaves the mount directory festering around
until the user clicks on cryptkeeper again to get a listing
of mounted/unmounted stashes (where rmdir <mountpoint> is called)

debian package: deps: encfs, fuse, zenity
